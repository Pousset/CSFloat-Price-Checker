# ── Clé API & variables d'environnement ──────────────────
.env
*.env
.env.local
.env.*.local

# ── Exports JSON générés par le script ───────────────────
exports_*.json
prices_*.json

# ── Build PyInstaller ─────────────────────────────────────
App/releases/dist/
App/releases/build/
App/releases/*.spec

# ── dist racine (garder le dossier, ignorer son contenu) ──
dist/*
!dist/.gitkeep

# ── Python ───────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
venv/
env/
.venv/
pip-log.txt

# ── OS ───────────────────────────────────────────────────
.DS_Store
Thumbs.db
